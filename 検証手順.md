# 新機能の検証手順

このセッションで追加した機能が正しく動くかを確認する手順です。
メニュー名・ボタン名は実装から取得した実際の表示文字列です。

検証用の素材は `検証動画/`（4本）と `vfx素材/`（8本）に置いてあります。

---

## 0. 準備

### ビルド
```
cmake --build build --config Release
```
実行バイナリ: `build\Release\v-simple-editor.exe`

### コマンドラインでの一括確認（GUI 操作なしで自動検証）
```
build\Release\v-simple-editor.exe --selftest=object-removal
build\Release\v-simple-editor.exe --selftest=deflicker
build\Release\v-simple-editor.exe --selftest=light3d
build\Release\v-simple-editor.exe --selftest=voice-isolation
build\Release\v-simple-editor.exe --selftest=batchexport-e2e
build\Release\v-simple-editor.exe --selftest=effect-library
build\Release\v-simple-editor.exe --selftest=vfx-generators
```
各コマンドは終了コード 0 なら全ゲート PASS です。
標準エラー出力に `PASS G1 ...` の形でゲートごとの結果が出ます。

**注意**: `light3d` はゲートを全て PASS した後、プロセス終了時にクラッシュする
既知の問題があります（`0xC0000005`）。ゲートの合否は標準エラー出力の
`summary: N PASS, 0 FAIL` で判断してください。詳細は
`.omc/handoffs/2026-08-03-mainwindow-teardown-crash.md` を参照。

---

## 1. オブジェクト除去（コンテンツに応じた塗りつぶし）

AE の Content-Aware Fill / Resolve の Object Removal 相当。

**メニュー**: ツール → 「オブジェクト除去（コンテンツに応じた塗りつぶし）…」

**前提（これを満たさないと起動時に弾かれます）**
1. `検証動画/01_オブジェクト除去_歩行者が横切る固定カメラ.mp4` を開き、V1 のクリップを選択
2. 通常のビデオクリップであること（ネストシーケンスは不可）
3. **そのクリップにクリップマスクまたはロトブラシマスクが付いていること**
   → 無い場合「選択クリップにクリップマスクまたはロトブラシマスクがありません。」で弾かれます
   → マスクは コンポジション メニューから作成します
4. マスクは歩行者を少し大きめに囲むように描く

**手順**
1. マスクを描いた状態で上記メニューを開く
2. 「マスクソース:」でマスクの種類を選ぶ
3. パラメータを確認（既定のままで可）
   - 参照フレーム半径: 前後何フレームを背景復元に使うか
   - 「背景整列（平面トラッキング）」: **既定 OFF のままにする**
     （カメラが動く素材でのみ使う。この検証動画は固定カメラなので不要）
   - マスク膨張 / 羽根: 境界の処理
4. 「プレビュー」を押して現在フレームの before / after を見る
5. 問題なければ「適用」

**成功の見分け方**
- マスク内から歩行者が消え、**そこに静止した背景が継ぎ目なく現れる**
- 背景の直線やタイル目地がマスクを跨いでズレずに繋がる
- **決定的な確認**: 歩行者が画面外にいるフレーム（0 フレーム目など）でプレビューすると、
  処理前と処理後がほぼ同一になる。ここで差分が出るなら、マスク領域を無条件に
  塗り潰しているだけで時間軸の背景復元が効いていない

**失敗時**
- マスク境界に人物のシルエットが薄く残る → マスク膨張と羽根を上げる
- 全体がぼやけた塗りになる → 参照フレームが足りない。半径を上げる

---

## 2. フリッカー除去

AE の Color Stabilizer / Resolve の Deflicker 相当。

**メニュー**: ツール → 「フリッカー除去...」

**前提**
- `検証動画/02_フリッカー除去_蛍光灯が明滅する倉庫.mp4` を開き、V1 のクリップを選択
- **マスクは不要**（オブジェクト除去と違い、マスク無しで起動できます）

**手順**
1. 上記メニューを開く
2. モードを選ぶ
   - **全体輝度**: 画面全体が一様に明滅する場合（この検証動画はこれ）
   - **RGB 個別**: 色被りを伴う明滅
   - **ローリングバンド**: 横帯状のフリッカー
3. 「解析」を押す → 輝度推移グラフが出る
4. 「適用」で書き出し。処理中は「中止」で止められます

**成功の見分け方**
- グラフで、**処理前の線がギザギザなのに対し、補正後の線がほぼ水平**になる
  - この素材はフレーム平均輝度が実測 29.9〜56.5 で振れています
- 補正後クリップを再生すると倉庫の壁面のチラつきが消える
- **決定的な確認**: 「補正強度」を 0% にして再解析すると、
  補正後の線が処理前の線に**完全に一致**する。これがスライダが実配線されている証拠

---

## 3. 3D ライト

AE の 3D ライトレイヤー / Fusion の 3D ライト相当。

**メニュー**: コンポジション → 「3D ライト…」

**前提**
- `検証動画/03_3Dライト_平坦照明の石膏像.mp4` を開く
- **マテリアル設定を触るにはビデオクリップの選択が必須**
  （未選択でもダイアログは開きますが、その欄が無効化されます。エラーは出ません）
- ライトは初期状態で 0 個なので、まず「追加」を押す
- 再生ヘッドがクリップの時間範囲上にあること

**手順**
1. 上記メニューを開き「追加」でライトを作る
2. 種別を選ぶ（Ambient / Parallel / Point / Spot）
3. 「色を選択」「強度」「減衰（Point / Spot）」を調整
4. クリップ側の「ライトを受ける」が ON であることを確認

**成功の見分け方**
- 平坦だった石膏像に**方向性のある陰影**が生まれる（片側が明るく反対が暗い）
- **決定的な確認**: Point または Spot を 1 個だけ足すと**画面全体が元より暗くなる**。
  そこに Ambient を足すと明るさが戻る。この「Point だけだと暗くなる／Ambient で戻る」
  挙動が、乗算シェーディングが実配線されている証拠です。
  明るさが全く変わらないなら陰影パスに入っていません
- 「有効」チェックの ON/OFF で元の平坦な絵と行き来できる
- Spot でコーン角を絞ると、光が当たる円の内外で明暗が分かれる

**書き出しにも反映されるか（重要）**
このセッションの修正で、ライトが**書き出しにも適用される**ようになりました。
プレビューで陰影を確認した後、実際に書き出して同じ陰影が出ることを確認してください。
プレビューと書き出しが一致するのがこのアプリの原則です。

---

## 4. 音声分離（スピーチ強調）

Premiere の「スピーチを強調」/ Resolve の Voice Isolation 相当。

**メニュー**: ツール → 「音声分離（スピーチ強調）…」

**前提**
- `検証動画/04_音声分離_カフェ騒音下の会話.mp4` を開く
- 対象は「選択中のオーディオクリップ → 選択中のビデオクリップ → ファイル選択ダイアログ」
  の順に決まります

**手順**
1. 上記メニューを開く
2. 出力モードを選ぶ（音声のみ / 背景のみ / ミックス）
3. 「分離強度:」を調整
4. 「解析」を押す → 「推定音声比率」が表示される
5. 「プレビュー再生」で処理後の音を聴く
6. 「適用」で WAV として保存

**成功の見分け方**
- **音声のみ**: ざわめきが後退し、会話が前に出る。**無音区間がスッと静かになる**のが分かりやすい
- **背景のみ**: 逆に会話がほぼ聞き取れなくなり、ざわめきだけが残る
- この 2 つが**相補的**であることが成功の証拠。両方とも元と同じに聞こえるなら効いていません
- 「推定音声比率」が 0% でも 100% でもない中間値になる
  （会話とノイズが混在する素材なので数十%台）
- パラメータを 1 つ動かすと「設定が変更されました。再解析してください。」に切り替わる

---

## 5. バッチ書き出し（修復済み）

Premiere の Media Encoder 相当。既存機能の欠陥を修正しました。

**メニュー**: ツール → バッチ書き出し

**修正された点**
- **fps が固定されなくなった**。以前は常に 30fps で書き出され、
  24fps / 60fps のプロジェクトは尺と動きが狂っていました
- **現在編集中のプロジェクトを追加できる**ようになった。
  以前は保存済み `.veditor` ファイルしか選べず、その経路では
  LUT・マスク・トラッキングが書き出しに反映されませんでした

**成功の見分け方**
- 24fps のプロジェクトをバッチ書き出しして、出力が 24fps であること
- LUT やマスクを適用したクリップをバッチ書き出しして、
  通常の書き出し（ファイル → エクスポート）と同じ絵になること

---

## 6. エフェクトライブラリ

AE の「エフェクト＆プリセット」/ Resolve の「Effects Library」相当。

**メニュー**: 表示 → 「エフェクトライブラリ」

**手順**
1. 上記メニューでドックを表示
2. 検索ボックスで絞り込み、カテゴリで分類を切り替え
3. サムネイルは**実際にそのエフェクトを適用した縮小画像**が出る
4. ダブルクリックまたはドラッグでクリップに適用
5. 「★お気に入り」を付けると次回起動時も保持される

**成功の見分け方**
- 既存のシェーダー 18 種・パーティクル 9 種・VFX ジェネレータ 7 種が
  **すべて並んでいる**こと（登録は自動走査なので、漏れがあれば実装の不具合）
- サムネイルが単なるアイコンではなく、効果が反映された画像であること
- エフェクトを組み合わせて「新規プリセットとして保存」し、
  それがライブラリに現れて再適用できること

---

## 7. VFX ジェネレータ

Blender の Elemental Asset Pack 相当を手続き的に生成します。

**メニュー**: 挿入 → 「VFX ジェネレータ…」

**7 種**: 爆発 / 稲妻 / 衝撃波 / エネルギービーム / 魔法陣 / マズルフラッシュ / エネルギーシールド

**手順**
1. 上記メニューを開き、種別を選ぶ
2. パラメータ（規模・色・シード等）を調整
3. タイムラインに追加

**成功の見分け方**
- 各ジェネレータが実際に描画される（透明なままなら不具合）
- **決定的な確認**: 同じシードで 2 回生成すると**完全に同じ絵**になる。
  シードを変えると形が変わる。これが決定論性の証拠で、
  プレビューと書き出しが一致するために必須です
- 時間を進めると形が変化する（静止画になっていない）
- 24fps と 60fps で同じ秒数を指定したとき、見た目が同じになる
  （フレーム番号ではなく秒で駆動されている証拠）

---

## 8. VFX 素材（黒背景フッテージの合成）

**素材**: `vfx素材/` に 8 本（炎・煙・爆発・火花・稲妻・水しぶき・塵・光漏れ）

**手順**
1. 素材を上のビデオトラックに置く
2. 合成モードを **Screen**（または加算）にする
3. 黒背景が抜けて、炎や煙だけが下の映像に乗る

**成功の見分け方**
- 黒い部分が完全に透明になり、下の映像がそのまま見える
- 黒が薄く乗る「黒浮き」が出る場合は、黒レベル引き締めのしきい値を上げる（既定 16）

詳細は `vfx素材/README.md` を参照してください。
